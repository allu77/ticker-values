# Rename to .env and replace with actual valies

GOOGLE_SPREADSHEET_ID=<Just the ID, not the full URL>
ALERT_EMAIL=<email address to receive CloudWatch alerts>

# Local runner only (not used by Lambda)
GOOGLE_CREDENTIALS_FILE=/path/to/service-account.json